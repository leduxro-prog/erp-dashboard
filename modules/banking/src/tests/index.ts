/**
 * Banking Module Tests
 *
 * Note: Tests are disabled for MVP deployment.
 * Unit tests with fixtures (ing-parser.test.ts, bt-parser.test.ts, parser-factory.test.ts, matching-service.test.ts)
 * can be uncommented when ready to add full test coverage.
 *
 * To run tests: npm test -- modules/banking
 */

// Export empty to keep this as a module
export {};
