# ==============================================================================
# Cypher ERP - RabbitMQ Event Bus
# Example Override Configuration
#
# Copy this file to override.tf for local development overrides.
# DO NOT commit override.tf to version control.
#
# Usage:
#   cp override.tf.example override.tf
#   # Edit override.tf as needed
#
# ==============================================================================

# Example: Override Terraform backend for local development
terraform {
  backend "local" {
    path = "terraform.tfstate"
  }
}

# Example: Override provider settings for local RabbitMQ via Docker
# provider "rabbitmq" {
#   endpoint = "http://localhost:15672"
#   username = "admin"
#   password = "admin"
#   insecure = true
# }

# Example: Override local values
# locals {
#   prefix = "cypher.local"
#   retry_ttls = {
#     attempt_1 = 5000   # 5 seconds for faster dev testing
#     attempt_2 = 15000  # 15 seconds
#     attempt_3 = 30000  # 30 seconds
#   }
# }
